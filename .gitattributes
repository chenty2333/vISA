# These files are immutable unified-diff carriers. Git's outer whitespace
# checker otherwise interprets their inner diff markers as source whitespace;
# the applied code remains checked with `git apply --whitespace=error-all`.
third_party/wacogo/patches/*.patch -whitespace
third_party/runtime-b-qualification/qualification/wacogo-patches/*.patch -whitespace

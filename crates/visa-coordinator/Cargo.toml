[package]
name = "visa-coordinator"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
visa-core = { path = "../visa-core" }
serde = { version = "1", default-features = false, features = ["alloc", "derive"] }

[lints]
workspace = true

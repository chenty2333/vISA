[package]
name = "target_abi"
version = "0.1.0"
publish = false
edition = "2024"

[lib]
path = "src/lib.rs"

[features]
default = ["hash"]
hash = ["dep:sha2"]

[dependencies]
sha2 = { version = "0.10", default-features = false, optional = true }

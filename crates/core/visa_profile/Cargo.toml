[package]
license.workspace = true
name = "visa_profile"
version = "0.2.0"
publish = false
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
contract_core = { path = "../contract_core" }
serde = { version = "1.0", default-features = false, features = ["alloc", "derive"] }

Read `README.md`, `docs/VISION.md`, `docs/ARCHITECTURE.md`, and
`docs/ROADMAP.md` before making architecture or scope decisions. They define
the current project boundary and capability sequence.
Use `docs/DEVELOPMENT.md` for supported commands and `docs/VALIDATION.md` for
claim and evidence rules.

When intent is ambiguous, state assumptions and ask before changing code.
Do not write speculative code, placeholder implementations, or workaround-only fixes.
If an implementation becomes large or tangled, simplify or refactor it while preserving the accepted scope.

Use Spec Kit as the active workflow for feature work. Historical material under
`docs/archive/` is background only. For the current feature, read
`specs/003-stage1-cooperative-handoff/plan.md` before changing scope.
